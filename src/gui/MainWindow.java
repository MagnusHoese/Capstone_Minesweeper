package gui;


import javax.swing.*;

public class MainWindow extends JFrame {
    public MainWindow() {

/*

        //getContentPane().setLayout(null);

        // Create PictureManager and Input instances

        System.out.println("HALLO");
        PictureManager pictureManager = new PictureManager();
        Input input = new Input(pictureManager);

        // For example, you might add the clickablePictures JLabels to a JFrame
        JFrame frame = new JFrame("Clickable Pictures");

        for (JLabel label : pictureManager.getClickablePictures()) {
            frame.add(label);
        }


        setSize(400, 400);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setVisible(true);

 */
    }
}
